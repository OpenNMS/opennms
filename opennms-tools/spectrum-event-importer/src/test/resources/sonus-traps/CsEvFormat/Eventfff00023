{d "%w- %d %m-, %Y - %T"} - A "empISDNCauseValuesCorrectedEvent" event has occurred, from {t} device, named {m}.

"Event generated when one of the ISDN cause values
            (which was outside of the range) goes back into the
            range specified for that particular object. "

empISDNCauseValuesEventInfo = {S 1}
(event [{e}])
