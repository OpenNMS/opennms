To build, run "mvn assembly:assembly".

This is a client receiver for the TCP RRD strategy.  It receives RRD updates
over IP and prints them to STDOUT.

Usage: java -jar csv-requisition-X.X.jar [port]
