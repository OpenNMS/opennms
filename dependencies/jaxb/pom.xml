<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <artifactId>dependencies</artifactId>
    <groupId>org.opennms</groupId>
    <version>36.0.3-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.opennms.dependencies</groupId>
  <artifactId>jaxb-dependencies</artifactId>
  <packaging>pom</packaging>
  <name>OpenNMS :: Dependencies :: JAXB</name>
  <dependencies>
    <dependency>
      <groupId>org.glassfish.jaxb</groupId>
      <artifactId>jaxb-runtime</artifactId>
      <version>${jaxbRuntimeVersion}</version>
    </dependency>
    <dependency>
      <groupId>org.eclipse.persistence</groupId>
      <artifactId>eclipselink</artifactId>
      <version>${eclipselinkVersion}</version>
      <exclusions>
          <exclusion>
              <groupId>org.eclipse.persistence</groupId>
              <artifactId>javax.persistence</artifactId>
          </exclusion>
      </exclusions>
    </dependency>
    <!-- EclipseLink 2.5.1's XMLBinaryDataHelper hard-references javax.mail.internet.MimeMultipart
         (constant-pool ldc, NoClassDefFoundError without it). mailapi is the javax.mail-namespace
         API without any transport providers, so it can't collide with jakarta.mail/angus-mail.
         Do not remove until EclipseLink is upgraded to a jakarta-namespace (3.x+) release. -->
    <dependency>
      <groupId>com.sun.mail</groupId>
      <artifactId>mailapi</artifactId>
    </dependency>
  </dependencies>
</project>
