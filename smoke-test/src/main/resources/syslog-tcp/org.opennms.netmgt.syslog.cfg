# Matches MINION_SYSLOG_TCP_PORT in MinionContainer.
syslog.tcp.listen.interface = 0.0.0.0
syslog.tcp.listen.port = 1601
syslog.tcp.framing = auto
# Batch one message at a time so the test does not wait out the batch interval.
syslog.batch.size = 1
syslog.batch.interval = 100
