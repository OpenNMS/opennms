<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <groupId>org.opennms</groupId>
    <artifactId>org.opennms.assemblies</artifactId>
    <version>36.0.0-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.opennms.assemblies</groupId>
  <artifactId>org.opennms.assemblies.daemon</artifactId>
  <name>OpenNMS Daemon</name>
  <packaging>pom</packaging>

  <properties>
    <license.skipAddThirdParty>true</license.skipAddThirdParty>
    <enforcer-skip-banned-dependencies>true</enforcer-skip-banned-dependencies>

    <NAME_DISPLAY>Daemon</NAME_DISPLAY>
    <NAME_LC>daemon</NAME_LC>
    <INSTPREFIX>/opt/daemon</INSTPREFIX>
    <RUNAS>daemon</RUNAS>
    <INITDIR>/etc/init.d</INITDIR>
    <SYSCONFDIR>/opt/daemon/etc</SYSCONFDIR>
    <PIDFILE>/opt/daemon/data/log/daemon.pid</PIDFILE>
  </properties>
  <build>
    <resources>
      <resource>
        <filtering>true</filtering>
        <directory>src/main/filtered-resources</directory>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-deploy-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <configuration>
           <encoding>UTF-8</encoding>
           <escapeString>\</escapeString>
           <delimiters>
             <delimiter>${*}</delimiter>
             <delimiter>@</delimiter>
           </delimiters>
           <useDefaultDelimiters>false</useDefaultDelimiters>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack-dependencies</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.opennms</groupId>
                  <artifactId>opennms-base-assembly</artifactId>
                  <version>${project.version}</version>
                  <classifier>daemon</classifier>
                  <type>tar.gz</type>
                  <outputDirectory>${project.build.directory}/unpacked/base-assembly</outputDirectory>
                  <includes>**/bin/**/*</includes>
                </artifactItem>
                <artifactItem>
                  <groupId>org.opennms.container</groupId>
                  <artifactId>org.opennms.container.shared</artifactId>
                  <version>${project.version}</version>
                  <type>tar.gz</type>
                  <outputDirectory>${project.build.directory}/unpacked/container-shared</outputDirectory>
                </artifactItem>
                <!-- Uses the sentinel Karaf container as a base runtime -->
                <artifactItem>
                  <groupId>org.opennms.features.container</groupId>
                  <artifactId>sentinel</artifactId>
                  <version>${project.version}</version>
                  <type>tar.gz</type>
                  <outputDirectory>${project.build.directory}/unpacked/container-daemon</outputDirectory>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <phase>prepare-package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <descriptors>
            <descriptor>src/assembly/daemon.xml</descriptor>
          </descriptors>
          <overrideUid>0</overrideUid>
          <overrideGid>0</overrideGid>
          <tarLongFileMode>posix</tarLongFileMode>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.opennms</groupId>
      <artifactId>opennms-base-assembly</artifactId>
      <version>${project.version}</version>
      <classifier>daemon</classifier>
      <type>tar.gz</type>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.opennms.container</groupId>
      <artifactId>org.opennms.container.shared</artifactId>
      <version>${project.version}</version>
      <type>tar.gz</type>
      <scope>runtime</scope>
    </dependency>
    <!-- Uses the sentinel Karaf container as a base runtime -->
    <dependency>
      <groupId>org.opennms.features.container</groupId>
      <artifactId>sentinel</artifactId>
      <version>${project.version}</version>
      <type>tar.gz</type>
      <scope>runtime</scope>
    </dependency>
  </dependencies>
</project>
