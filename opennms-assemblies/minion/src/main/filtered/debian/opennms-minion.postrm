#!/bin/sh

set -e

ETCDIR="/etc/minion"
MINIONHOME="/usr/share/minion"

if [ "$1" = "purge" ]; then
	rm -rf "${MINIONHOME}" || true
	rm -rf "${ETCDIR}" || true
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
