#!/bin/sh

set -e

ETCDIR="/opt/sentinel/etc"
SENTINELHOME="/opt/sentinel"

if [ "$1" = "purge" ]; then
	rm -rf "${SENTINELHOME}" || true
	rm -rf "${ETCDIR}" || true
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
