<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.3.0"
    xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.5.0"
    xsi:schemaLocation="
		http://www.osgi.org/xmlns/blueprint/v1.0.0 
		https://osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
		http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.3.0
		http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.3.0.xsd
		http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.5.0
		http://aries.apache.org/schemas/blueprint-ext/blueprint-ext-1.5.xsd
">

    <cm:property-placeholder id="kafkaConsumerProperties" persistent-id="org.opennms.core.ipc.sink.kafka.consumer" placeholder-prefix="[[" placeholder-suffix="]]" update-strategy="reload">
        <cm:default-properties>
            <cm:property name="group.id" value="OpenNMS"/>
        </cm:default-properties>
    </cm:property-placeholder>

    <reference id="configurationAdmin" interface="org.osgi.service.cm.ConfigurationAdmin" availability="mandatory" />

    <!-- Config Provider -->
    <bean id="kafkaConfigProvider" class="org.opennms.core.ipc.common.kafka.OsgiKafkaConfigProvider" >
        <argument value="[[group.id]]"/>
        <!-- same as persistent-id -->
        <argument value="org.opennms.core.ipc.sink.kafka.consumer"/>
        <argument ref="configurationAdmin" />
    </bean>
    <bean id="kafkaSinkMetricRegistry" class="com.codahale.metrics.MetricRegistry"/>
    <service ref="kafkaSinkMetricRegistry" interface="com.codahale.metrics.MetricSet" >
        <service-properties>
            <entry key="name" value="Kafka Sink"/>
            <entry key="description" value="Metrics related to Sink messages between OpenNMS and Minion"/>
        </service-properties>
    </service>


    <reference id="tracerRegistry" interface="org.opennms.core.tracing.api.TracerRegistry"/>

    <reference id="identity" interface="org.opennms.distributed.core.api.Identity"/>

    <!-- ConsumerManager -->
    <bean id="kafkaMessageConsumerManager" class="org.opennms.core.ipc.sink.kafka.server.KafkaMessageConsumerManager" init-method="afterPropertiesSet"
          destroy-method="shutdown">
        <argument ref="kafkaConfigProvider" />
        <argument ref="identity"/>
        <argument ref="tracerRegistry"/>
        <argument ref="kafkaSinkMetricRegistry"/>
    </bean>
    <service ref="kafkaMessageConsumerManager" interface="org.opennms.core.ipc.sink.api.MessageConsumerManager" />

    <!-- Offset Provider (Consumer) -->
    <bean id="kafkaLagMetricRegistry" class="com.codahale.metrics.MetricRegistry"/>
    <service ref="kafkaLagMetricRegistry" interface="com.codahale.metrics.MetricSet" >
        <service-properties>
            <entry key="name" value="Kafka Lag"/>
            <entry key="description" value="Metrics related to Kafka Lag"/>
        </service-properties>
    </service>

    <bean id="kafkaOffsetProvider" class="org.opennms.core.ipc.sink.kafka.server.offset.KafkaOffsetProvider"
          init-method="start" destroy-method="stop">
        <argument ref="kafkaConfigProvider"/>
        <argument ref="kafkaLagMetricRegistry"/>
    </bean>

    <!-- Kafka HealthCheck for Sink (Sentinel) -->
    <service interface="org.opennms.core.health.api.HealthCheck">
        <bean class="org.opennms.core.ipc.common.kafka.KafkaHealthCheck" >
            <argument ref="kafkaConfigProvider"/>
            <argument value="Sink Consumer" />
        </bean>
    </service>

</blueprint>
