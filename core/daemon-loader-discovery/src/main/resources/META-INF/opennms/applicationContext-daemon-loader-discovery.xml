<?xml version="1.0" encoding="UTF-8"?>
<!--
  Karaf-only Discovery loader context.

  In a Karaf-only container (no Manager, no Eventd), this context replaces the
  Spring hierarchy with a flat context that wires Discovery beans directly to
  OSGi services.

  All dependencies come from OSGi service registry:
  - EventIpcManager from KafkaEventIpcManagerAdapter (event-forwarder-kafka feature)
  - InterfaceToNodeCache from DAO feature
  - LocationAwarePingClient from RPC ICMP feature
  - LocationAwareDetectorClient from provisioning feature (optional)
  - DiscoveryConfigFactory from opennms-config feature
-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:osgi="http://www.springframework.org/schema/osgi"
       xmlns:onmsgi="http://xmlns.opennms.org/xsd/spring/onms-osgi"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd
           http://www.springframework.org/schema/osgi http://www.springframework.org/schema/osgi/spring-osgi.xsd
           http://xmlns.opennms.org/xsd/spring/onms-osgi http://xmlns.opennms.org/xsd/spring/onms-osgi.xsd
       ">

    <!-- Enable @Autowired processing for Discovery and DiscoveryTaskExecutorImpl -->
    <context:annotation-config />

    <!-- Shared KafkaRpcClientFactory wiring -->
    <import resource="classpath:kafka-rpc-client-factory.xml"/>

    <!-- ServiceRegistry: import the shared instance from OSGi (registered by
         service-registry module) rather than creating a local one with
         <onmsgi:default-registry/> which would be a different, empty instance -->
    <osgi:reference id="serviceRegistry"
                    interface="org.opennms.core.soa.ServiceRegistry"/>

    <!-- ============================================================== -->
    <!-- OSGi Service References (from other Karaf features)            -->
    <!-- ============================================================== -->

    <!-- Event infrastructure (from opennms-event-forwarder-kafka blueprint) -->
    <osgi:reference id="eventIpcManager"
                    interface="org.opennms.netmgt.events.api.EventIpcManager"/>

    <!-- Discovery configuration — instantiated directly (not an interface,
         so cannot use onmsgi:reference JDK proxy). Constructor calls reload()
         which reads discovery-configuration.xml from opennms.home/etc/. -->
    <bean id="discoveryConfigFactory"
          class="org.opennms.netmgt.config.DiscoveryConfigFactory"/>

    <!-- InterfaceToNodeCache (from DAO feature) -->
    <onmsgi:reference id="interfaceToNodeCache"
                      interface="org.opennms.netmgt.dao.api.InterfaceToNodeCache"/>

    <onmsgi:reference id="distPollerDao"
                      interface="org.opennms.netmgt.dao.api.DistPollerDao"/>
    <onmsgi:reference id="entityScopeProvider"
                      interface="org.opennms.core.mate.api.EntityScopeProvider"/>

    <!-- Ping client — real RPC via KafkaRpcClientFactory to Minion -->
    <bean id="pingerFactory"
          class="org.opennms.netmgt.icmp.best.BestMatchPingerFactory"/>
    <bean id="pingProxyRpcModule"
          class="org.opennms.netmgt.icmp.proxy.PingProxyRpcModule"/>
    <bean id="pingSweepRpcModule"
          class="org.opennms.netmgt.icmp.proxy.PingSweepRpcModule"/>
    <bean id="locationAwarePingClient"
          class="org.opennms.netmgt.icmp.proxy.LocationAwarePingClientImpl"/>

    <!-- Detector client — real RPC via KafkaRpcClientFactory to Minion -->
    <bean id="serviceDetectorRegistry"
          class="org.opennms.core.daemon.loader.LocalServiceDetectorRegistry"/>
    <bean id="scanExecutor" class="java.util.concurrent.Executors"
          factory-method="newCachedThreadPool"/>
    <bean id="detectorClientRpcModule"
          class="org.opennms.netmgt.provision.detector.client.rpc.DetectorClientRpcModule"/>
    <bean id="locationAwareDetectorClient"
          class="org.opennms.netmgt.provision.detector.client.rpc.LocationAwareDetectorClientRpcImpl"/>

    <!-- ============================================================== -->
    <!-- Local Beans (from discovery context)                           -->
    <!-- ============================================================== -->

    <bean id="unmanagedInterfaceFilter"
          class="org.opennms.netmgt.discovery.UnmanagedInterfaceFilter">
        <constructor-arg ref="interfaceToNodeCache"/>
    </bean>

    <bean id="rangeChunker"
          class="org.opennms.netmgt.discovery.RangeChunker">
        <constructor-arg ref="unmanagedInterfaceFilter"/>
    </bean>

    <bean id="discoveryTaskExecutor"
          class="org.opennms.netmgt.discovery.DiscoveryTaskExecutorImpl"/>

    <!-- Export DiscoveryTaskExecutor as OSGi service -->
    <onmsgi:service interface="org.opennms.netmgt.discovery.DiscoveryTaskExecutor"
                    ref="discoveryTaskExecutor"/>

    <!-- ============================================================== -->
    <!-- Discovery Daemon                                               -->
    <!-- ============================================================== -->

    <bean id="daemon" class="org.opennms.netmgt.discovery.Discovery"/>

    <!-- ============================================================== -->
    <!-- Daemon Lifecycle (replaces Manager)                            -->
    <!-- ============================================================== -->

    <bean name="daemonLifecycleManager"
          class="org.opennms.core.daemon.loader.DaemonLifecycleManager">
        <constructor-arg ref="daemon"/>
    </bean>

</beans>
