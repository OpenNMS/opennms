<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-2.0.xsd">

    <changeSet id="36.0.0-add-alarm-event-denormalization" author="opennms">
        <comment>Denormalize event data onto alarms for Kafka-backed event pipeline</comment>

        <addColumn tableName="alarms">
            <column name="event_tsid" type="BIGINT"/>
            <column name="event_uei" type="VARCHAR(256)"/>
            <column name="event_source" type="VARCHAR(256)"/>
            <column name="event_severity" type="INTEGER"/>
            <column name="event_timestamp" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="event_node_id" type="BIGINT"/>
            <column name="event_log_msg" type="TEXT"/>
            <column name="last_event_data" type="TEXT"/>
        </addColumn>

        <createIndex tableName="alarms" indexName="idx_alarms_event_tsid">
            <column name="event_tsid"/>
        </createIndex>
    </changeSet>

    <changeSet id="36.0.0-backfill-alarm-event-data" author="opennms">
        <comment>Backfill denormalized event data from existing events table</comment>
        <sql>
            UPDATE alarms a SET
                event_tsid = e.eventid,
                event_uei = e.eventuei,
                event_source = e.eventsource,
                event_severity = e.eventseverity,
                event_timestamp = e.eventtime,
                event_node_id = e.nodeid,
                event_log_msg = e.eventlogmsg
            FROM events e
            WHERE a.lasteventid = e.eventid
              AND a.event_tsid IS NULL;
        </sql>
    </changeSet>

    <changeSet id="36.0.0-drop-alarm-event-fk" author="opennms">
        <comment>Drop FK, index, and lasteventid column from alarms — events move to Kafka.
            Must drop node_alarms view first since it depends on lasteventid.</comment>
        <dropView viewName="node_alarms"/>
        <dropForeignKeyConstraint baseTableName="alarms"
                                  constraintName="fk_eventidak2"/>
        <dropIndex tableName="alarms" indexName="alarm_eventid_idx"/>
        <dropColumn tableName="alarms" columnName="lasteventid"/>

        <createView replaceIfExists="true" viewName="node_alarms">
                SELECT
                n.nodeid,
                n.nodecreatetime,
                n.nodeparentid,
                n.nodetype,
                n.nodesysoid,
                n.nodesysname,
                n.nodesysdescription,
                n.nodesyslocation,
                n.nodesyscontact,
                n.nodelabel,
                n.nodelabelsource,
                n.nodenetbiosname,
                n.nodedomainname,
                n.operatingsystem,
                n.lastcapsdpoll,
                n.foreignsource,
                n.foreignid,
                n.location,
                a.alarmid,
                a.eventuei,
                a.ipaddr,
                a.reductionkey,
                a.alarmtype,
                a.counter,
                a.severity,
                a.firsteventtime,
                a.lasteventtime,
                a.firstautomationtime,
                a.lastautomationtime,
                a.description,
                a.logmsg,
                a.operinstruct,
                a.tticketid,
                a.tticketstate,
                a.suppresseduntil,
                a.suppresseduser,
                a.suppressedtime,
                a.alarmackuser,
                a.alarmacktime,
                a.managedobjectinstance,
                a.managedobjecttype,
                a.applicationdn,
                a.ossprimarykey,
                a.x733alarmtype,
                a.qosalarmstate,
                a.clearkey,
                a.ifindex,
                a.stickymemo,
                a.systemid,
                (a.alarmacktime NOTNULL) AS acknowledged,
                COALESCE(s_cat.categoryname, 'no category') AS categoryname,
                s_cat.categorydescription,
                s.servicename,
                nas.max_alarm_severity,
                nas.max_alarm_severity_unack,
                nas.alarm_count_unack,
                nas.alarm_count
                FROM
                node n
                JOIN
                alarms a
                ON
                n.nodeid = a.nodeid
                JOIN
                node_alarm_status nas
                ON
                a.nodeid = nas.nodeid
                LEFT JOIN
                service s
                ON
                a.serviceid = s.serviceid
                LEFT JOIN
                category_node cat
                ON
                n.nodeid = cat.nodeid
                LEFT JOIN
                categories s_cat
                ON
                cat.categoryid = s_cat.categoryid
        </createView>
    </changeSet>

    <changeSet id="36.0.0-drop-events-table" author="opennms">
        <comment>Remove events table — events are now stored in Kafka</comment>
        <dropTable tableName="events" cascadeConstraints="true"/>
        <sql>DROP SEQUENCE IF EXISTS eventsnxtid;</sql>
    </changeSet>

    <changeSet id="36.0.0-create-events-archive" author="opennms">
        <comment>Create events_archive table — materialized view of Kafka fault events for REST, UI, and reporting queries</comment>

        <createTable tableName="events_archive">
            <column name="event_tsid" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="event_uei" type="VARCHAR(256)">
                <constraints nullable="false"/>
            </column>
            <column name="event_source" type="VARCHAR(256)"/>
            <column name="event_severity" type="INTEGER"/>
            <column name="event_time" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="node_id" type="BIGINT"/>
            <column name="ip_addr" type="VARCHAR(64)"/>
            <column name="service_name" type="VARCHAR(256)"/>
            <column name="event_log_msg" type="TEXT"/>
            <column name="event_descr" type="TEXT"/>
            <column name="event_display" type="VARCHAR(1)" defaultValue="Y"/>
            <column name="event_log" type="VARCHAR(1)" defaultValue="Y"/>
            <column name="ifindex" type="INTEGER"/>
            <column name="event_data" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()"/>
        </createTable>

        <sql>CREATE INDEX idx_events_archive_time ON events_archive (event_time DESC)</sql>
        <createIndex tableName="events_archive" indexName="idx_events_archive_uei">
            <column name="event_uei"/>
        </createIndex>
        <createIndex tableName="events_archive" indexName="idx_events_archive_node">
            <column name="node_id"/>
        </createIndex>
        <createIndex tableName="events_archive" indexName="idx_events_archive_node_ifindex">
            <column name="node_id"/>
            <column name="ifindex"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
