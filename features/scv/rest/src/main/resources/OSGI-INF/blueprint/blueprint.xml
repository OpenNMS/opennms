<blueprint
        xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd"
>
    <reference id="secureCredentialsVault" interface="org.opennms.features.scv.api.SecureCredentialsVault" availability="mandatory"/>

    <bean id="defaultScvRestService" class="org.opennms.features.scv.rest.DefaultScvRestService">
        <argument ref="secureCredentialsVault"/>
    </bean>

    <!--
        Registered for the OSGi JAX-RS Whiteboard (OSGi cmpn R7, chapter 151).
        Replaces the "application-path" property of the (unmaintained) osgi-jax-rs-connector:
        the resource attaches itself to the named application which defines the base URI.
    -->
    <service interface="org.opennms.features.scv.rest.ScvRestService" ref="defaultScvRestService">
        <service-properties>
            <entry key="osgi.jaxrs.resource" value="true" />
            <entry key="osgi.jaxrs.name" value="scv" />
            <entry key="osgi.jaxrs.application.select" value="(osgi.jaxrs.name=opennms-rest)" />
        </service-properties>
    </service>
</blueprint>
