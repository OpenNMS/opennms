<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
           xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.1.0"
           xsi:schemaLocation="
		http://www.osgi.org/xmlns/blueprint/v1.0.0
		http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd

		http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0
		http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.1.0.xsd

		http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.1.0
		http://aries.apache.org/schemas/blueprint-ext/blueprint-ext-1.1.xsd
">

    <reference id="flowConfiguration" interface="org.opennms.netmgt.flows.config.FlowConfiguration" availability="mandatory" />
    <reference id="credentialsProvider" interface="org.opennms.plugins.elasticsearch.rest.credentials.CredentialsProvider" availability="mandatory" />

    <!-- properties -->
    <bean id="retryCooldown" factory-ref="flowConfiguration" factory-method="getRetryCooldown"/>

    <!-- Proxy for the JestClientFactory -->
    <bean id="restClientConfig" class="org.opennms.netmgt.flows.elastic.RestClientConfigurationFactory" factory-method="create" >
        <argument ref="flowConfiguration"/>
    </bean>
    <bean id="flowRequestExecutorFactory" class="org.opennms.netmgt.flows.elastic.FlowRequestExecutor">
        <argument ref="retryCooldown"/>
    </bean>
    <bean id="clientFactory" class="org.opennms.plugins.elasticsearch.rest.RestClientFactory">
        <argument ref="restClientConfig" />
        <property name="credentials" ref="credentialsProvider" />
        <property name="requestExecutorFactory" ref="flowRequestExecutorFactory" />
    </bean>

    <!-- Actually creates the client, but only once -->
    <bean id="jestClient" factory-ref="clientFactory" factory-method="createClient" destroy-method="shutdownClient"/>

    <bean id="indexStrategyString" factory-ref="flowConfiguration" factory-method="getElasticIndexStrategy" />
    <bean id="indexStrategy" class="org.opennms.netmgt.flows.elastic.IndexStrategyFactory" factory-method="createIndexStrategy">
        <argument ref="indexStrategyString"/>
    </bean>

    <bean id="indexSettings" class="org.opennms.netmgt.flows.config.IndexSettings" factory-ref="flowConfiguration" factory-method="getIndexSettings" />

    <!-- The repository -->
    <bean id="elasticFlowRepository" class="org.opennms.netmgt.flows.elastic.ElasticFlowRepository">
        <argument ref="jestClient"/>
        <argument ref="indexStrategy"/>
    </bean>
    <!-- Proxy it, to ensure initialization on first call of any method -->
    <bean id="initializingElasticFlowRepository" class="org.opennms.netmgt.flows.elastic.InitializingFlowRepository">
        <argument ref="elasticFlowRepository" />
        <argument ref="jestClient" />
        <argument ref="indexSettings" />
    </bean>
    <service interface="org.opennms.netmgt.flows.api.FlowRepository" ref="initializingElasticFlowRepository">
        <service-properties>
            <entry key="registration.export" value="true" />
        </service-properties>
    </service>

</blueprint>
