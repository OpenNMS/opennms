<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>subscription</artifactId>
    <groupId>org.opennms.features</groupId>
    <version>21.0.0-SNAPSHOT</version>
  </parent>

  <groupId>org.opennms</groupId>
  <artifactId>productkeys.executor</artifactId>
  <packaging>pom</packaging>
  <name>OpenNMS :: Features :: ProductKeys :: Executor</name>

  <modelVersion>4.0.0</modelVersion>

  <properties>
    <!-- productkeys.properties (if supplied) are referenced in the top level opennms folder -->
    <productKeysFile>${project.basedir}/../../../productkeys.properties</productKeysFile>
  </properties>

  <modules>
    <module>feature</module>
  </modules>

  <build>
    <plugins>

      <!--reading in product keys file to generate subscription -->
      <plugin>
        <groupId>org.codehaus.gmaven</groupId>
        <artifactId>gmaven-plugin</artifactId>
        <inherited>false</inherited>
        <executions>
          <execution>
            <id>properties-for-productkeys-executor</id>
            <phase>initialize</phase>
            <goals>
              <goal>execute</goal>
            </goals>
            <configuration>
              <providerSelection>2.0</providerSelection>
              <!-- common script is in the parent subscription folder -->
              <source>${project.basedir}/../src/main/script/subscriptionPropertiesScript.groovy</source>
             </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- <plugin> -->
      <!-- <groupId>org.codehaus.mojo</groupId> -->
      <!-- <artifactId>properties-maven-plugin</artifactId> -->
      <!-- <version>1.0.0</version> -->
      <!-- <executions> -->
      <!-- <execution> -->
      <!-- <phase>initialize</phase> -->
      <!-- <goals> -->
      <!-- <goal>read-project-properties</goal> -->
      <!-- </goals> -->
      <!-- <configuration> -->
      <!-- <files> -->
      <!-- <file>../productkeys.properties</file> -->
      <!-- </files> -->
      <!-- </configuration> -->
      <!-- </execution> -->
      <!-- </executions> -->
      <!-- </plugin> -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.6.0</version>
        <inherited>false</inherited>
        <executions>
          <execution>
            <phase>process-sources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <executable>mvn</executable>
          <arguments>
            <!-- <argument>-X</argument> -->
            <argument>-f</argument>
            <argument>${project.basedir}/pom-exec.xml</argument>
            <argument>clean</argument>
            <argument>${goalStr}</argument> <!-- install, deploy or "" (empty) -->
            <argument>-DproductName=${subscriptionName}</argument>
            <argument>-DproductVersion=${subscriptionVersion}</argument>
            <argument>-DproductGroupId=${subscriptionGroupId}</argument>

            <argument>-DregenerateSubscription=${regenerateSubscription}</argument>
            <argument>-DlicencemanagerVersion=${licencemanagerVersion}</argument>

          </arguments>
        </configuration>

      </plugin>
    </plugins>
  </build>

</project>