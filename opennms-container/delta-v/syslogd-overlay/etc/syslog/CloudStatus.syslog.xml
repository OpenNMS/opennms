<?xml version="1.0"?>
<!--
  Syslog match rules for cloud service status messages.
  The RadixTreeSyslogParser extracts "CLOUD-STATUS" as the process name
  (from the colon in the syslog format), so we match on process-match
  and then match the message body for the service name and status.
-->
<syslogd-configuration-group>
  <ueiList>
    <ueiMatch>
      <process-match expression="^CLOUD-STATUS$"/>
      <match type="regex" expression="^.*Service (\S+) is (Down)$"/>
      <uei>uei.opennms.org/syslogd/cloud/serviceDown</uei>
      <parameter-assignment matching-group="1" parameter-name="cloudService"/>
      <parameter-assignment matching-group="2" parameter-name="cloudStatus"/>
    </ueiMatch>
    <ueiMatch>
      <process-match expression="^CLOUD-STATUS$"/>
      <match type="regex" expression="^.*Service (\S+) is (Up)$"/>
      <uei>uei.opennms.org/syslogd/cloud/serviceUp</uei>
      <parameter-assignment matching-group="1" parameter-name="cloudService"/>
      <parameter-assignment matching-group="2" parameter-name="cloudStatus"/>
    </ueiMatch>
  </ueiList>
</syslogd-configuration-group>
