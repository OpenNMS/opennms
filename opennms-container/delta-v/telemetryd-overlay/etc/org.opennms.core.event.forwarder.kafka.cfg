# Kafka Event Forwarder configuration for standalone Telemetryd (daemon container)
bootstrap.servers=kafka:9092
topic.name=opennms-fault-events
consumer.group.id=opennms-telemetryd
poll.timeout.ms=100
ipc.topic.name=opennms-ipc-events
