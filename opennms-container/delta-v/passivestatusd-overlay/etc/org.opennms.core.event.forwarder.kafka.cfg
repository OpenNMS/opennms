# Kafka Event Forwarder configuration for standalone PassiveStatusd (daemon container)
bootstrap.servers=kafka:9092
topic.name=opennms-fault-events
consumer.group.id=opennms-passivestatusd
poll.timeout.ms=100
