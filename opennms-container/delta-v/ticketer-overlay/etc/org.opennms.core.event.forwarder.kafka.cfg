# Kafka Event Forwarder configuration for standalone TroubleTicketer (daemon container)
bootstrap.servers=kafka:9092
topic.name=opennms-fault-events
consumer.group.id=opennms-ticketer
poll.timeout.ms=100
