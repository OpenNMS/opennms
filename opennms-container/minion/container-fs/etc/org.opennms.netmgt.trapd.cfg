trapd.listen.interface = ${env:MINION_TRAPD_LISTEN_INTERFACE:-0.0.0.0}
trapd.listen.port = ${env:MINION_TRAPD_LISTEN_PORT:-1162}
trapd.useAddressFromVarbind = ${env:MINION_TRAPD_USE_ADDRESS_FROM_VARBIND:-false}
