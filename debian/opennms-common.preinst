#! /bin/sh
# postrm script for opennms-common
#
# vim:set ai et sts=4 sw=4 tw=0:

if [ -d /etc/opennms  ]; then
    echo "Backing up existing /etc/opennms ..."
    cp -a /etc/opennms /etc/opennms.backup.$(date +%s)
fi

exit 0