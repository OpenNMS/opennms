export interface State {
  types: any
  provisionDService: any
  sendModifiedData: any
  toast: string
}

const state: State = {
  types: [],
  provisionDService: null,
  sendModifiedData: null,
  toast: ''
}

export default state
