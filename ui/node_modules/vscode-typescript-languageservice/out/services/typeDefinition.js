"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.register = void 0;
const transforms_1 = require("../utils/transforms");
const shared = require("@volar/shared");
function register(languageService, getTextDocument, getTextDocument2) {
    return (uri, position) => {
        const document = getTextDocument(uri);
        if (!document)
            return [];
        const fileName = shared.uriToFsPath(document.uri);
        const offset = document.offsetAt(position);
        const entries = languageService.getTypeDefinitionAtPosition(fileName, offset);
        if (!entries)
            return [];
        return transforms_1.entriesToLocationLinks([...entries], getTextDocument2);
    };
}
exports.register = register;
//# sourceMappingURL=typeDefinition.js.map