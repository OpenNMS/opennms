= Related Events

{page-component-title} correlates possible related events from the same node in ongoing alarms.
To do this events with the same `node ID`, `interface`, `service`, and `ifIndex` are being used.

.Alarm Related Events page
image::alarms/alarm_related-events.png["Alarm Related Events page displaying related events", 850]
