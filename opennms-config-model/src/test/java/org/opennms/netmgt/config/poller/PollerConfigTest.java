package org.opennms.netmgt.config.poller;

import org.junit.Ignore;
import org.opennms.core.test.xml.XmlTest;

@Ignore
public class PollerConfigTest extends XmlTest<PollerConfiguration> {

    public PollerConfigTest(final PollerConfiguration sampleObject, final String sampleXml, final String schemaFile) {
        super(sampleObject, sampleXml, schemaFile);
    }

}
