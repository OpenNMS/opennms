commands:
  trivy-analyze:
    steps:
      - attach_workspace:
          at: ~/project
      - run:
          name: what is in workspace
          command: |
            echo "Workspace contents:"
            ls -la ~/project/
      - run:
          name: What is under artifacts
          command: |
            echo "Artifacts contents:"
            ls -la ~/project/artifacts/
        
