 jobs:
  smoke-test-sentinel:
    executor: smoke-test-executor
    parallelism: 8
    resource_class: medium+
    steps:
      - cached-checkout
      - attach_workspace:
          at: ~/
      - run-smoke-tests:
          suite: sentinel
          rerun-failtest-count: 1
