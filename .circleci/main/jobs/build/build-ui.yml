jobs:
  build-ui:
    executor: ui-executor
    steps:
      - checkout
      - run:
          name: Build
          command: |
            cd ui && pnpm install && pnpm lint && pnpm build:all && pnpm test
