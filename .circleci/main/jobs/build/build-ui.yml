jobs:
  build-ui:
    executor: ui-executor
    steps:
      - checkout
      - run:
          name: Prebuild
          command: |
            yarn global add node-gyp@9.4.1
      - run:
          name: Build
          command: |
            cd ui && yarn install && yarn test && yarn build
