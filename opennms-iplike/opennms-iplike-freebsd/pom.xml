<?xml version="1.0" encoding="UTF-8"?><project>
  <parent>
    <artifactId>opennms-iplike</artifactId>
    <groupId>org.opennms</groupId>
    <version>1.3.2-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>opennms-iplike-freebsd</artifactId>
  <packaging>so</packaging>
  <name>OpenNMS iplike Stored Procedure for PostgreSQL - FreeBSD</name>
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>native-maven-plugin</artifactId>
        <extensions>true</extensions>
        <configuration>
          <compilerProvider>generic</compilerProvider>
          <compilerStartOptions>
            <compilerStartOption>-fPIC -O ${build.postgresql.compiler.arg}</compilerStartOption>
          </compilerStartOptions>
          <sources>
            <source>
              <directory>${build.postgresql.include.dir}</directory>
            </source>
            <source>
              <directory>../src/main/native</directory>
              <fileNames>
                <fileName>iplike.c</fileName>
              </fileNames>
            </source>
          </sources>
          <linkerStartOptions>
            <linkerStartOption>-shared ${build.postgresql.compiler.arg} ${build.postgresql.linker.arg} -lc</linkerStartOption>
          </linkerStartOptions>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
